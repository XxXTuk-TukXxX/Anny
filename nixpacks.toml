[phases.setup]
# System dependencies required by OCRmyPDF at runtime.
# (Install the Python deps via requirements.txt as usual.)
nixPkgs = ["tesseract", "ghostscript"]

[start]
cmd = "gunicorn -c gunicorn.conf.py flask_app:app"
